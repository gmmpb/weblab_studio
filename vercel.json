{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": {
        "installCommand": "pnpm install patch-package && pnpm install",
        "buildCommand": "pnpm run build"
      }
    }
  ]
}
